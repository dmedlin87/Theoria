"""Legacy namespace placeholder.

The historic ``theo.services`` package has been decomposed into
``theo.domain.services``, ``theo.application.services``, and
``theo.infrastructure``. Import modules from the new packages instead of this
namespace."""
